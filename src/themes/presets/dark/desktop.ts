import { ThemeWhitepaper } from 'react-elegant-ui/esm/theme';

// Share layout/typography tokens with the light preset; only color differs.
import '../default/root.css';
import './color.css';

export const theme: ThemeWhitepaper = {
	color: 'dark',
	root: 'default',
};
